import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { Navigation } from "@/components/navigation"
import { Footer } from "@/components/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Get Client Builder - AI Cold Email Automation That Gets Results",
  description:
    "Get 5+ new clients in 7 days with our done-for-you AI cold email campaigns. $200 flat fee, guaranteed results, pay with MetaMask.",
  keywords: "AI cold email, lead generation, client acquisition, business automation",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navigation />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
